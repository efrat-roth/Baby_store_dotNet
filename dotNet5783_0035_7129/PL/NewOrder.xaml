<Window x:Class="PL.NewOrder"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ValueConverterDemo"
        mc:Ignorable="d"
        Title="NewOrder" Height="450" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource self}}">

    <Window.Resources>
        <local:NotBooleanToVisibilityConverter x:Key="notBooleanToVisibilityConverter"/>
    </Window.Resources>

    <Grid Name="MainGrid" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Name="UpGrid"  Grid.Row="0"
        VerticalAlignment="Stretch" Width="auto">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions >
            <Label Content="Category:" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" FontSize="16"  />
            <ComboBox x:Name="CategorySelector" Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" SelectionChanged="CategroyFilter" ItemsSource="{Binding _Category}">
                <ComboBox.Background>
                    <LinearGradientBrush EndPoint="0,1">
                        <GradientStop Color="#FFF0F0F0"/>
                        <GradientStop Color="#FFF395D6" Offset="1"/>
                    </LinearGradientBrush>
                </ComboBox.Background>
            </ComboBox>
        </Grid>
        <Grid Grid.Row="1" >
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <ListView x:Name="ProductsListView"  Grid.Row="0" d:ItemsSource="{d:SampleData ItemCount=5 }"  
                   Background="#FFFBE6F4" FontSize="12" MouseDoubleClick="DetailsOfProduct"  ItemsSource="{Binding ProductsLists }" 
                      HorizontalContentAlignment="Stretch" >

                <ListView.View >
                    <GridView>
                        <GridViewColumn Header="ID" Width="auto" x:Name="idP" DisplayMemberBinding="{Binding ID}"/>
                        <GridViewColumn Header="Name" Width="auto" DisplayMemberBinding="{Binding Name}"/>
                        <GridViewColumn Header="Price" Width="auto" DisplayMemberBinding="{Binding Price}"/>
                        <GridViewColumn Header="Categoy" Width="auto" DisplayMemberBinding="{Binding Category}"/>

                        <GridViewColumn Header="InStock">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox Width="auto" IsEnabled="False" IsChecked="{Binding InStock}" Name="inS"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Amount in cart" Width="auto" x:Name="AmountCart" DisplayMemberBinding="{Binding AmountInCart}"/>

                    </GridView>
                </ListView.View>
            </ListView>

            <Grid x:Name="ProductsByCategory" Grid.Row="0"  Background="#FFFBE6F4" 
                   Visibility="{Binding Path= IsChecked, ElementName=CategoryCheck,Converter={StaticResource notBooleanToVisibilityConverter}}" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Label Content="Clothes:" Grid.Row="0" Grid.Column="0" HorizontalAlignment="Center"/>
                <ListView x:Name="ClothesBy"  Background="#FFFBE6F4" Grid.Row="1" Grid.Column="0" MouseDoubleClick="DetailsOfProduct" ItemsSource="{Binding _ByCategory[0] }">
                    <ListView.View >
                        <GridView>
                            <GridViewColumn Header="ID" Width="auto"  DisplayMemberBinding="{Binding ID}"/>
                            <GridViewColumn Header="Name" Width="auto" DisplayMemberBinding="{Binding Name}"/>
                            <GridViewColumn Header="Price" Width="auto" DisplayMemberBinding="{Binding Price}"/>
                            <GridViewColumn Header="InStock">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox Width="auto" IsEnabled="False" IsChecked="{Binding InStock}" Name="inS"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Amount in cart" Width="auto"  DisplayMemberBinding="{Binding AmountInCart}"/>

                        </GridView>
                    </ListView.View>
                </ListView>
                <Label Content="Bottles:" Grid.Row="2" Grid.Column="0" HorizontalAlignment="Center"/>
                <ListView x:Name="BottlesBy" Background="#FFFBE6F4" Grid.Row="3" Grid.Column="0" MouseDoubleClick="DetailsOfProduct"  ItemsSource="{Binding _ByCategory[1] }">
                    <ListView.View >
                        <GridView>
                            <GridViewColumn Header="ID" Width="auto"  DisplayMemberBinding="{Binding ID}"/>
                            <GridViewColumn Header="Name" Width="auto" DisplayMemberBinding="{Binding Name}"/>
                            <GridViewColumn Header="Price" Width="auto" DisplayMemberBinding="{Binding Price}"/>
                            <GridViewColumn Header="InStock">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox Width="auto" IsEnabled="False" IsChecked="{Binding InStock}" Name="inS"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Amount in cart" Width="auto"  DisplayMemberBinding="{Binding AmountInCart}"/>

                        </GridView>
                    </ListView.View>
                </ListView>
                <Label Content="Toys" Grid.Row="4" Grid.Column="0" HorizontalAlignment="Center"/>
                <ListView x:Name="ToysBy" Background="#FFFBE6F4" Grid.Row="5" Grid.Column="0" MouseDoubleClick="DetailsOfProduct"  ItemsSource="{Binding _ByCategory[2] }">
                    <ListView.View >
                        <GridView>
                            <GridViewColumn Header="ID" Width="auto"  DisplayMemberBinding="{Binding ID}"/>
                            <GridViewColumn Header="Name" Width="auto" DisplayMemberBinding="{Binding Name}"/>
                            <GridViewColumn Header="Price" Width="auto" DisplayMemberBinding="{Binding Price}"/>
                            <GridViewColumn Header="InStock">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox Width="auto" IsEnabled="False" IsChecked="{Binding InStock}" Name="inS"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Amount in cart" Width="auto"  DisplayMemberBinding="{Binding AmountInCart}"/>

                        </GridView>
                    </ListView.View>
                </ListView>
                <Label Content="Socks:" Grid.Row="0" Grid.Column="1" HorizontalAlignment="Center"/>
                <ListView x:Name="SocksBy" Background="#FFFBE6F4" Grid.Row="1" Grid.Column="1" MouseDoubleClick="DetailsOfProduct"  ItemsSource="{Binding _ByCategory[3] }">
                    <ListView.View >
                        <GridView>
                            <GridViewColumn Header="ID" Width="auto"  DisplayMemberBinding="{Binding ID}"/>
                            <GridViewColumn Header="Name" Width="auto" DisplayMemberBinding="{Binding Name}"/>
                            <GridViewColumn Header="Price" Width="auto" DisplayMemberBinding="{Binding Price}"/>
                            <GridViewColumn Header="InStock">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox Width="auto" IsEnabled="False" IsChecked="{Binding InStock}" Name="inS"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Amount in cart" Width="auto"  DisplayMemberBinding="{Binding AmountInCart}"/>

                        </GridView>
                    </ListView.View>
                </ListView>
                <Label Content="Accesories:" Grid.Row="2" Grid.Column="1" HorizontalAlignment="Center"/>
                <ListView x:Name="AcceoriesBy" Background="#FFFBE6F4" Grid.Row="3" Grid.Column="1" MouseDoubleClick="DetailsOfProduct"  ItemsSource="{Binding _ByCategory[4] }">
                    <ListView.View >
                        <GridView>
                            <GridViewColumn Header="ID" Width="auto"  DisplayMemberBinding="{Binding ID}"/>
                            <GridViewColumn Header="Name" Width="auto" DisplayMemberBinding="{Binding Name}"/>
                            <GridViewColumn Header="Price" Width="auto" DisplayMemberBinding="{Binding Price}"/>
                            <GridViewColumn Header="InStock">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox Width="auto" IsEnabled="False" IsChecked="{Binding InStock}" Name="inS"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Amount in cart" Width="auto"  DisplayMemberBinding="{Binding AmountInCart}"/>

                        </GridView>
                    </ListView.View>
                </ListView>
                <Label Content="Baby carriages:" Grid.Row="4" Grid.Column="1"/>
                <ListView x:Name="Babycarriages" Background="#FFFBE6F4" Grid.Row="5" Grid.Column="1" MouseDoubleClick="DetailsOfProduct"  ItemsSource="{Binding _ByCategory[5] }">
                    <ListView.View >
                        <GridView>
                            <GridViewColumn Header="ID" Width="auto"  DisplayMemberBinding="{Binding ID}"/>
                            <GridViewColumn Header="Name" Width="auto" DisplayMemberBinding="{Binding Name}"/>
                            <GridViewColumn Header="Price" Width="auto" DisplayMemberBinding="{Binding Price}"/>
                            <GridViewColumn Header="InStock">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox Width="auto" IsEnabled="False" IsChecked="{Binding InStock}" Name="inS"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Amount in cart" x:Name="a" Width="auto"  DisplayMemberBinding="{Binding AmountInCart}"/>

                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
            <Grid Background="#FFFBE6F4" Grid.Row="1 ">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <Label Content="Press twice on product to see details&#xD;&#xA;                 put v to show by category" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Stretch"  FontSize="12" Background="White" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Opacity="0.7"  />
                <Button  x:Name="Cart" Content="Go to the cart" HorizontalAlignment="Right" HorizontalContentAlignment="Center"  Grid.Column="2" Click="ShowCart" Background="#FFDADADA" Opacity="0.8"  Width="130" />
                <CheckBox x:Name="CategoryCheck" Grid.Column="1" VerticalAlignment="Bottom" HorizontalAlignment="Center"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
